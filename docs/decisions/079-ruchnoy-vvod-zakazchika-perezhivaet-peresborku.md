+++
{
  "title": "Ручной ввод заказчика переживает пересборку книги",
  "date": "2026-09-18",
  "kind": "решение",
  "authority": "заказчик",
  "status": "действует",
  "tags": ["публикация", "набор-данных"],
  "verify": "test: tests/test_delivery_fill.py::test_the_carry_covers_every_column_not_just_occupancy",
  "invalidates_on": ["решение заказчика"],
  "evidence": [
    "measurement: пересборкой стёрты I2:I4 — наполненность по прибытию, заполненная заказчиком вручную; копии не нашлось, значения утрачены",
    "measurement: проверка на живых 295 строках — четыре клетки ручного ввода (наполненность в трёх строках и маршрут 225 в четвёртой) пережили полную пересборку",
    "measurement: приметы с маршрутом развалились ровно на той строке, где заказчик маршрут и вписал: перенос потерял 1 строку из 295",
    "file:src/paxcount/delivery/fill.py",
    "file:tests/test_delivery_fill.py"
  ],
  "origin": "по ходу",
  "supersedes": []
}
+++

Пересборка книги **не стирает клетку, которую сама не заполняет**. Правило общее
на все графы, а не список из тех, где однажды попались.

## Почему это правило, а не исправление

Книга лежит у заказчика, и он в ней пишет. Наполненность по прибытию мы не
считаем вовсе (решение 032) — её он заполняет руками, глядя запись. Стёртая
пересборкой клетка не выглядит потерей: графа и должна быть пустой с нашей
стороны, и заметить пропажу можно, только помня, что ты туда что-то вписывал.

Перечислять «защищённые графы» поимённо нельзя: знать заранее, где заказчик
решит дописать своё, мы не можем. Поэтому мерится не имя графы, а факт — пусто
ли там у нас.

## Наше значение главнее

Клетку, которую заполняем мы, перенос не трогает. Иначе исправление данных
никогда не доехало бы до книги: однажды записанное значение держалось бы вечно.

## Книгу пишет сборка, а не рука поверх неё

Копирование готового файла в папку заказчика — дыра мимо всей защиты: перенос
отработал, а следом `cp` лёг поверх свежей правки. Поэтому у сборки есть
`--book`: она читает книгу заказчика и в неё же пишет, одним действием.
Не сопоставилась хоть одна строка — книга заказчика не трогается вовсе.

## Строка узнаётся по приметам, а не по номеру

Машина, пропущенная оператором, вписывается в ленту по времени (решение 069),
и после вставки прежний номер строки указывает на соседнюю машину. Перенос по
номеру положил бы чужую наполненность не на ту машину — тихо и правдоподобно.
Приметы — часы, минуты и номер ТС, и в них входит ТОЛЬКО то, что пишем мы.
Маршрут был в приметах ровно один прогон: заказчик вписал его в строку, где мы
маршрут не опознали, — и строка перестала узнаваться. Ручной ввод, ради
которого перенос заведён, ломал сам перенос. Одинаковые приметы бывают
(повторное нажатие, решение 075) и разбираются по порядку следования.
