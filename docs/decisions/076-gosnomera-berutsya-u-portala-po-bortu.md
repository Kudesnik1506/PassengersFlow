+++
{
  "title": "Госномера берутся у портала по бортовому номеру",
  "date": "2026-09-18",
  "kind": "решение",
  "authority": "заказчик",
  "status": "действует",
  "tags": ["публикация", "набор-данных"],
  "verify": "test: tests/test_delivery_plates.py::test_the_portal_plate_fills_the_state_number",
  "invalidates_on": ["решение заказчика"],
  "evidence": [
    "measurement: 148 уникальных бортов за сутки, портал вернул госномер для 141; в книге 286 госномеров из 295 строк, было 6",
    "measurement: портал не знает 7 бортов — 382252 (испорченная запись), 5379, 5480, 7160, 7203, 7301, 7563; у них в графе остаётся бортовой",
    "measurement: в книге 286 жёлтых граф номера — ровно столько подстановок",
    "measurement: у 12 машин за день две смены на разных маршрутах — смена выбиралась по времени прибытия",
    "file:src/paxcount/delivery/plates.py",
    "file:tests/test_delivery_plates.py"
  ],
  "origin": "по ходу",
  "supersedes": []
}
+++

Автобусу в графу номера идёт государственный номер, полученный у портала по
бортовому. Это рабочий путь решения 026, до сих пор не подключённый к сборке
книги: клиент портала был написан и лежал без дела.

## Три запрета, на которых держится замена

**Не выдумывать.** Портал не знает машину — в графе остаётся бортовой. Так и в
принятом заказчиком файле: «34547 -нет в базе». Из 148 бортов портал не знал 7.

**Не затирать.** Бортовой остаётся в строке: он единственный ключ, по которому
спор о машине разбирается потом (решение 026), и единственный вход в
справочник — обратный поиск по госномеру портал не поддерживает.

**Не молчать.** Маршрут, о котором портал говорит иначе, записывается в
`overrides` как расхождение, а не подменяет наш. Третье мнение не становится
согласованным оттого, что оно третье.

## Подстановка красится жёлтым

Я предлагал не красить: номер получен из государственного справочника по
бортовому самого оператора, спорить в нём не с чем, а 286 жёлтых строк заливают
книгу почти целиком. Владелец решил иначе, и правило у него проще моего: графа
изменена нами — значит помечена. Проверяющий, сверяя книгу с выгрузкой, видит в
графе не то, что записал оператор, и обязан знать, чья это подмена.

Строка, которой портал не знает, не метится: в графе остался бортовой оператора,
и менять там было нечего.

## Кэш обязателен

Ответы ложатся в `out/portal/<ОП>.json` и поднимаются на следующем прогоне.
Портал государственный, доступ общий на нескольких расшифровщиков, а книга
пересобирается по многу раз за день: без кэша это 148 одинаковых запросов на
каждую пересборку. В репозиторий кэш не уходит — в нём настоящие госномера.
