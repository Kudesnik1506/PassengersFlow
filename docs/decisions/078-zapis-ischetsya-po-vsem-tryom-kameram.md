+++
{
  "title": "Запись для строки ищется по всем трём камерам, разрыв называется словами",
  "date": "2026-09-18",
  "kind": "решение",
  "authority": "заказчик",
  "status": "действует",
  "tags": ["публикация", "визиты", "набор-данных"],
  "verify": "test: tests/test_coverage.py::test_a_row_inside_the_reference_gap_is_filmed_by_another_camera",
  "invalidates_on": ["решение заказчика", "сменилась камера или ракурс"],
  "evidence": [
    "measurement: К2 теряет 1074 с (07:33:45, 12 записей оператора внутри), 98 с и 38 с; К1 за все три смены не имеет ни одного разрыва",
    "measurement: строк без имени файла было 14 из 295, стало 0 — 280 сняты К2, 14 К1, одна К3; замечаний приёмки было 64, стало 50",
    "measurement: заметку о разрыве получили 12 строк; стыки в 4 с (08:49:29, 18:17:10) отсечены порогом 5 с",
    "file:src/paxcount/delivery/timeline.py",
    "file:src/paxcount/delivery/coverage.py",
    "file:tests/test_coverage.py"
  ],
  "origin": "по ходу",
  "supersedes": []
}
+++

В графу N идёт файл ЛЮБОЙ камеры, писавшей в этот момент, а не только опорной.
Порядок предпочтения — считающие вперёд: К2, К3, К1.

## Почему не одной К2

У К2 три разрыва за утро, главный — 1074 с с двенадцатью записями оператора
внутри. К1 в это же время писала без единого перерыва за все три смены. Пустая
графа означала бы «не снято» там, где снято: четырнадцать строк книги были
предъявлены как непокрытые, хотя запись есть.

## Поправка часов берётся на камеру, а не на файл

`clocks` хранит поправку на файл и запрещает одалживать её у соседа. Здесь она
взята на камеру, и это названное допущение: смена — одна сплошная запись,
нарезанная одними часами (решение 027), а не набор независимых файлов.

**Цена допущения названа прямо: у К1 поправка получена одной визуальной парой,
и разброса у неё нет — сравнить не с чем.** Имя файла эту неточность переживёт
(файлы по 10-20 минут), а время в графе P сдвинется ровно на ошибку поправки.
Как только появится вторая независимая пара по К1, величину надо пересчитать.

## Разрыв называется, даже когда его закрыла другая камера

Заказчик просил отражать аномалию съёмки в комментарии, называя камеру и время.
Закрытый разрыв не перестал быть аномалией: именно он объясняет, почему у
соседних строк файлы разных камер. Стык короче пяти секунд заметкой не
становится — это технический шум, и порог у книги тот же, что у отчёта о
покрытии (`coverage.MIN_GAP_S`, один на обоих потребителей).
